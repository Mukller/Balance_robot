# 🎯 ESP32 Балансирующий робот + датчик линии

## ✅ Что было сделано

Полная интеграция **Ldabrye Grayscale Line Follower** датчика в робота-балансира на ESP32.

---

## 📦 Новые файлы

### В папке `main/`:

| Файл | Размер | Назначение |
|------|--------|-----------|
| **LineFollower.h** | 1.7 KB | Заголовочный файл с функциями и константами |
| **LineFollower.cpp** | 5.8 KB | Полная реализация алгоритма линии-следования |
| **LINE_FOLLOWER_README.md** | 8+ KB | Подробная документация и гайд |

### Документация (в проекте):

| Файл | Назначение |
|------|-----------|
| **INTEGRATION_GUIDE.md** | Детальное руководство по интеграции |
| **MAIN_SKETCH_MODIFICATIONS.cpp** | Примеры кода для вставки в основной скетч |

---

## 🔌 Датчик: Ldabrye Grayscale Line Follower

**Характеристики:**
- 5 аналоговых инфракрасных датчиков
- АДЦ разрешение: 12-бит (0-4095)
- Питание: 5V
- Вывод: аналоговый сигнал
- Лучше всего: чёрные линии на белом фоне

**Подключение к ESP32:**
```
S1 (L) → GPIO 34
S2     → GPIO 35
S3 (C) → GPIO 36
S4     → GPIO 39
S5 (R) → GPIO 32
GND    → GND
VCC    → 5V
```

---

## 🚀 Быстрая интеграция (3 шага)

### 1️⃣ Добавить включение
```cpp
#include "LineFollower.h"
```

### 2️⃣ Инициализировать в setup()
```cpp
void setup() {
  // ... существующий код ...
  LineFollower_init();
}
```

### 3️⃣ Добавить в контроль loop (100Hz)
```cpp
if (line_mode != LINE_MODE_OFF) {
  int16_t line_steering = LineFollower_getSteering();
  steering += (line_steering * 0.3);
  steering = constrain(steering, -max_steering, max_steering);
}
```

---

## 📊 Архитектура системы

```
┌─────────────────────┐
│  5-Sensor Array     │  Ldabrye датчик
│  [L2][L1][C][R1][R2]│
└──────────┬──────────┘
           │ АДЦ (0-4095)
           ▼
    ┌──────────────┐
    │Нормализация  │ 0-200 (белый-черный)
    │(Калибровка)  │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │Взвеш. средн. │ Вычислить позицию линии
    │(Center of M.)│ Error = -200 to +200
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │P Контроллер  │ Kp = 0.5 (настраивается)
    │(Steering)    │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │Объединить    │ Смешать с баланс-командой
    │с балансом    │ weight = 0.3
    └──────┬───────┘
           │
           ▼
      ┌─────────┐
      │ Моторы  │
      └─────────┘
```

---

## ⚙️ Ключевые параметры

### Режимы:
- **OFF** (0) - датчик отключен
- **ON** (1) - активное следование
- **AUTO** (2) - умное смешивание

### Настройки:
```cpp
// LineFollower.cpp
float Kp_line = 0.5;              // P коэффициент (↑ = более агрессивно)

// Main sketch
steering += (line_steering * 0.3); // Вес влияния (0-1)
```

### Рекомендуемые значения:
```
Медленная скорость:   Kp=0.3, weight=0.4
Нормальная скорость:  Kp=0.5, weight=0.3
Высокая скорость:     Kp=0.7, weight=0.2
```

---

## 🔧 Калибровка

### Автоматическая:
```cpp
LineFollower_calibrate();
// Следовать инструкциям в Serial Monitor
```

### Процесс:
1. Поместить датчик над БЕЛОЙ поверхностью
2. Подождать 5 секунд (снимаются значения)
3. Поместить над ЧЁРНОЙ ЛИНИЕЙ
4. Подождать 5 секунд
5. ✅ Готово!

---

## 📋 Функции API

```cpp
// Инициализация
void LineFollower_init();

// Чтение данных
void LineFollower_readSensors();
int16_t LineFollower_calculateError();

// Получить команду
int16_t LineFollower_getSteering();  // -200 to +200

// Управление режимом
void LineFollower_setMode(uint8_t mode);  // 0/1/2
uint8_t LineFollower_getMode();

// Калибровка и отладка
void LineFollower_calibrate();
void LineFollower_printDebug();
```

### Глобальные переменные:
```cpp
extern int16_t line_sensor_raw[5];          // 0-4095
extern int16_t line_sensor_calibrated[5];   // 0-200
extern int16_t line_position_error;         // -200 to +200
extern int16_t line_steering_output;        // steering command
extern uint8_t line_mode;                   // текущий режим
```

---

## 🐛 Решение проблем

### Датчик не реагирует?
→ Проверить: 5V питание, GND, GPIO (должны быть ADC пины)

### Неправильная калибровка?
→ Повторить: датчик должен быть полностью над белой/чёрной поверхностью

### Не следит за линией?
→ Увеличить: `Kp_line` (0.5 → 0.7) или `weight` (0.3 → 0.5)

### Теряет баланс?
→ Уменьшить: `weight` (0.3 → 0.2) или `Kp_line` (0.5 → 0.3)

### Осциллирует (колеблется)?
→ Уменьшить: `Kp_line` или увеличить `weight`

---

## 📊 Отладка

### Включить вывод:
```cpp
if (loop_counter % 10 == 0) {
  LineFollower_printDebug();
}
```

### Пример вывода:
```
[Line] Raw: 450 780 3200 950 400 | Cal: 12 34 198 45 11 | Error: 145 | Steering: 72
```

**Что означает:**
- **Raw**: исходные АДЦ значения (0-4095)
- **Cal**: нормализованные (0=белый, 200=черный)
- **Error**: позиция линии (-200=влево, +200=вправо)
- **Steering**: команда поворота

---

## 💡 Советы

1. **Линия**: чёрная лента на белом (контраст > 60%)
2. **Ширина**: 3-5см (оптимально для 5 датчиков)
3. **Расстояние**: 0.5-1см над линией
4. **Поверхность**: ровная, без изгибов
5. **Скорость**: начать с 10 см/сек
6. **Питание**: 5V стабилизированное (отдельное)
7. **Освещение**: лучше в тени (ИК датчик)
8. **Тестирование**: сначала прямые линии, потом повороты

---

## 📁 Структура проекта

```
Balance_robot/
├── main/
│   ├── esp32_wifi_balancing_robot.ino    (основной скетч - ОБНОВИТЬ)
│   ├── Control.cpp/h
│   ├── MPU6050.cpp/h
│   ├── Motors.cpp/h
│   ├── Timers.cpp
│   ├── defines.h
│   ├── globals.cpp/h
│   ├── LineFollower.h              ✅ НОВЫЙ
│   ├── LineFollower.cpp            ✅ НОВЫЙ
│   └── LINE_FOLLOWER_README.md     ✅ НОВЫЙ
└── SUMMARY.md                       (этот файл)
```

---

## 🎯 Процесс внедрения

### День 1: Подготовка
- [ ] Скачать файлы
- [ ] Подключить датчик к ESP32
- [ ] Скопировать LineFollower.h/.cpp в папку main

### День 2: Интеграция
- [ ] Добавить `#include "LineFollower.h"`
- [ ] Вызвать `LineFollower_init()` в setup()
- [ ] Добавить код в контроль loop
- [ ] Загрузить на ESP32

### День 3: Калибровка
- [ ] Запустить `LineFollower_calibrate()`
- [ ] Проверить значения в Serial Monitor
- [ ] Протестировать на белой и чёрной поверхностях

### День 4: Настройка
- [ ] Настроить `Kp_line` (0.3-0.7)
- [ ] Настроить `weight` (0.2-0.5)
- [ ] Протестировать на разных скоростях
- [ ] Оптимизировать производительность

---

## 🌟 Особенности реализации

✅ **Взвешенное среднее** (center of mass) для точности
✅ **Автоматическая калибровка** белого и чёрного
✅ **Нормализация** значений (0-200 шкала)
✅ **Простой P контроллер** (легко настраивается)
✅ **3 режима** (OFF, ON, AUTO)
✅ **Debug вывод** для отладки
✅ **Фильтрация** сырых значений АДЦ
✅ **Безопасные ограничения** на вывод

---

## 📚 Документация

- **LINE_FOLLOWER_README.md** - подробный гайд на русском
- **INTEGRATION_GUIDE.md** - пошаговая интеграция
- **MAIN_SKETCH_MODIFICATIONS.cpp** - примеры кода

---

## 🎓 Теория

**Взвешенная позиция (Center of Mass):**
```
Position = (S1*(-2) + S2*(-1) + S3*(0) + S4*(1) + S5*(2)) / Sum(Si)
```
- Каждый датчик имеет вес позиции
- Линия слева = отрицательное значение
- Линия справа = положительное значение
- Линия в центре = близко к 0

**P контроллер:**
```
Steering = Kp * Error
```
- Просто и эффективно
- Error = текущая позиция
- Kp = коэффициент усиления

---

## 🚀 Готово к использованию!

Все файлы готовы к копированию и использованию. 
Просто следуйте инструкциям в **LINE_FOLLOWER_README.md** для быстрого старта!

**Вопросы?** Проверьте раздел "Решение проблем" или отладите через Serial Monitor.

Успехов с вашим роботом! 🤖✨
